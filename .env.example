# Google Calendar API Configuration
# Using public calendar + API key (no service account needed)

# Get this from Google Cloud Console > APIs & Services > Credentials
# Create API Key and optionally restrict to Google Calendar API only
GOOGLE_API_KEY=your_api_key_here

# Get this from Google Calendar Settings > Integrate calendar
# Format: your-calendar@group.calendar.google.com or username@gmail.com
CALENDAR_ID=your-calendar-id@group.calendar.google.com

# Note: These values can be shared with demo participants
# The API key is public and rate-limited, not a security risk
